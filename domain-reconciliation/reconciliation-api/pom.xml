<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.acme.platform</groupId>
        <artifactId>domain-reconciliation</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>reconciliation-api</artifactId>
    <name>reconciliation-api</name>
    <description>
        對帳業務域之 API + Service 層。
        包含:
          - ReconciliationResource (JAX-RS)
          - ReconciliationCsvExporter (CSV 匯出)
          - AuditLogService (供 saga2-transfer-workflow 對帳記錄使用)
        供 reconciliation-app 與 transfer-workflow 共用。
    </description>

    <dependencies>
        <dependency>
            <groupId>org.acme.platform</groupId>
            <artifactId>platform-common</artifactId>
        </dependency>
        <dependency>
            <groupId>org.acme.platform</groupId>
            <artifactId>platform-data-index</artifactId>
        </dependency>

        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-arc</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-jsonp</artifactId>
        </dependency>
        <dependency>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
        </dependency>

        <!-- 供 AuditLogService 對 SQLite 寫入 (同一個 reconciliation.db) -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
        </dependency>
    </dependencies>
</project>
