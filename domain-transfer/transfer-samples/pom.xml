<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.acme.platform</groupId>
        <artifactId>domain-transfer</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>transfer-samples</artifactId>
    <name>transfer-samples</name>
    <description>
        示範性質的 workflow 集合 — OpenAPI-workflow / java-workflow / rest-workflow。
        與業務無關,僅用於驗證 SonataFlow 各類 function 呼叫模式可運作。
        生產環境若不需要這些 demo workflow,可在 distribution 模組的 pom 直接排除本模組依賴。
    </description>

    <dependencies>
        <dependency>
            <groupId>org.acme.platform</groupId>
            <artifactId>transfer-api</artifactId>
        </dependency>

        <!-- Kogito 引擎 (與 transfer-workflow 相同) -->
        <dependency>
            <groupId>org.kie</groupId>
            <artifactId>kie-addons-quarkus-process-management</artifactId>
        </dependency>
        <dependency>
            <groupId>org.kie</groupId>
            <artifactId>kie-addons-quarkus-source-files</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.kie.sonataflow</groupId>
            <artifactId>sonataflow-quarkus</artifactId>
        </dependency>
    </dependencies>
</project>
